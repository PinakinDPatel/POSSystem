<Window x:Class="POSSystem.Department"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:POSSystem"
      mc:Ignorable="d" 
      WindowStyle="None" WindowStartupLocation="CenterScreen" Height="600" Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15*"></RowDefinition>
            <RowDefinition Height="20*"></RowDefinition>
            <RowDefinition Height="65*"></RowDefinition>
        </Grid.RowDefinitions>
            <Label Content="Department" FontSize="32" Grid.ColumnSpan="4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Content="Department Name :-" HorizontalAlignment="Center"  VerticalAlignment="Bottom" FontSize="14"/>
            <TextBox Name="TxtDepartment" HorizontalAlignment="Center" Height="32" VerticalAlignment="Top" Width="140" FontSize="14" Grid.Row="1">
                <TextBox.Effect>
                    <DropShadowEffect BlurRadius="2" ShadowDepth="3" Color="#FFE6D4FD"/>
                </TextBox.Effect>
            </TextBox>
            <Label Name="lblDeptId" Visibility="Hidden"></Label>
            <Button Name="btnDeptSave" Content="Save" Width="95" Height="33" Foreground="White" Grid.Column="3" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="28" Click="Button_Click"/>
            <Label Content="Department Code :-"  VerticalAlignment="Bottom" FontSize="14" Grid.Column="1" HorizontalAlignment="Center"/>
            <TextBox x:Name="TxtDepartment_Code" HorizontalAlignment="Center" Height="32" VerticalAlignment="Top" Width="140" FontSize="14" Grid.Column="1" Grid.Row="1">
                <TextBox.Effect>
                    <DropShadowEffect BlurRadius="2" ShadowDepth="3" Color="#FFE6D4FD"/>
                </TextBox.Effect>
            </TextBox>
            <TextBox x:Name="TxtTaxRate" HorizontalAlignment="Center" Height="32" VerticalAlignment="Top" Width="140" FontSize="14" Grid.Column="2" Grid.Row="1">
                <TextBox.Effect>
                    <DropShadowEffect BlurRadius="2" ShadowDepth="3" Color="#FFE6D4FD"/>
                </TextBox.Effect>
            </TextBox>
            <Label Content="TaxRate :-" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="14" Grid.Column="2"/>
           
        </Grid>
        <Label Name="lblusername" Visibility="Hidden"/>
        <DataGrid Name="DeptGrid" Grid.Row="2" HorizontalAlignment="Center"   VerticalAlignment="Top" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Department}" ClipboardContentBinding="{x:Null}" Header="Name" Width="150*"/>
                <DataGridTextColumn Binding="{Binding DepartmentCode}" ClipboardContentBinding="{x:Null}" Header="Code" Width="50*"/>
                <DataGridTextColumn Binding="{Binding TaxRate}" ClipboardContentBinding="{x:Null}" Header="TaxRate" Width="50*"/>
                <DataGridTemplateColumn Header="Edit" Width="50*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="onEdit" Background="White">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="Image/Edit1.png" Width="25" Height="25"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Delete" Width="50*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="onDelete" Background="White">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="Image/Delete.png" Width="25" Height="25"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
